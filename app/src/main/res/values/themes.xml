<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.TestProject" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.TestProject" parent="Base.Theme.TestProject" />

    <style name="Theme.AppCompat.Start" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="postSplashScreenTheme">@style/Theme.TestProject</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_news_avd_v02</item>
    </style>
</resources>